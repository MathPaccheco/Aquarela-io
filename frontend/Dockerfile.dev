# Aquarela.io — Frontend development image (Vite HMR)
# Use docker-compose.yml which mounts the source as a volume.

FROM node:20-alpine

WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci

# Source is mounted at runtime via docker-compose volume
EXPOSE 5173

CMD ["npm", "run", "dev"]
