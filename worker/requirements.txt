# Runtime dependencies — pinned for reproducibility
aio-pika==9.4.3          # RabbitMQ AMQP client (native asyncio)
numpy==1.26.4             # Matrix operations for fluid simulation (Phase 6)
scipy>=1.13               # Gaussian diffusion via scipy.ndimage.gaussian_filter
asyncpg==0.29.0           # PostgreSQL async driver for chunk grid initialisation
python-dotenv==1.0.1      # Load .env variables
